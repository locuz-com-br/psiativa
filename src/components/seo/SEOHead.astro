---
import { SITE_CONFIG } from "../../config/site.config";

interface Props {
  title: string;
  description: string;
  /** Optional i18n key for the <title>; swapped client-side on language toggle. */
  titleKey?: string;
  /** Optional i18n key for the meta description; swapped client-side on language toggle. */
  descriptionKey?: string;
  canonicalPath?: string;
  image?: string;
  type?: "website" | "article";
  noindex?: boolean;
  schema?: Record<string, unknown> | Array<Record<string, unknown>>;
}

const {
  title,
  description,
  titleKey,
  descriptionKey,
  canonicalPath = Astro.url.pathname,
  image = SITE_CONFIG.seo.ogImage,
  type = "website",
  noindex = false,
  schema,
} = Astro.props;

const siteUrl = SITE_CONFIG.url;
const siteName = SITE_CONFIG.name;
const fullTitle = title.includes(siteName) ? title : `${title} | ${siteName}`;
const canonicalUrl = new URL(canonicalPath, siteUrl).toString();
const imageUrl = new URL(image, siteUrl).toString();
const schemaItems = Array.isArray(schema) ? schema : schema ? [schema] : [];
---

<title data-i18n={titleKey}>{fullTitle}</title>
<meta name="description" content={description} data-i18n-content={descriptionKey} />
<meta name="robots" content={noindex ? "noindex, nofollow" : "index, follow"} />
<link rel="canonical" href={canonicalUrl} />

<!-- OG / Social -->
<meta property="og:title" content={fullTitle} data-i18n-content={titleKey} />
<meta property="og:description" content={description} data-i18n-content={descriptionKey} />
<meta property="og:type" content={type} />
<meta property="og:url" content={canonicalUrl} />
<meta property="og:site_name" content={siteName} />
<meta property="og:image" content={imageUrl} />
<meta property="og:locale" content={SITE_CONFIG.defaultLocale.replace("-", "_")} />
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content={fullTitle} data-i18n-content={titleKey} />
<meta name="twitter:description" content={description} data-i18n-content={descriptionKey} />
<meta name="twitter:image" content={imageUrl} />

{schemaItems.map((item) => (
  <script type="application/ld+json" set:html={JSON.stringify(item).replace(/</g, "\\u003c")} />
))}
