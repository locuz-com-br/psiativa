/**
 * CMS SDK initialization.
 * Replace the placeholder below with your actual CMS client (e.g. Contentful, Sanity, Hygraph).
 */

// Example – uncomment and configure for your CMS:
// import { createClient } from 'contentful';
// export const cmsClient = createClient({
//   space: import.meta.env.CONTENTFUL_SPACE_ID,
//   accessToken: import.meta.env.CONTENTFUL_ACCESS_TOKEN,
// });

export const cmsClient = null; // TODO: replace with real client
